# Clones. Rebuildable, large, never committed.
.cache/

# Local escape hatch (SPEC 5.3). Never shared.
decisions.local.yml

node_modules/
dist/
ui/dist/
*.log

# Local agent tooling cache.
.atl/

# NOTE: out/ is intentionally NOT ignored.
# The dataset is the product and is committed (SPEC 5.1).

# CodeGraph local index. Per-checkout, must never be shared -- its root and
# checked-out bytes differ per worktree.
.codegraph/
